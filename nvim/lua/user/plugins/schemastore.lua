-- A Neovim Lua plugin providing access to the SchemaStore catalog.
-- This plugin validates json schemas like vscode does.
local M = {
  "b0o/schemastore.nvim",
  lazy = true,
}

function M.config() end

return M
